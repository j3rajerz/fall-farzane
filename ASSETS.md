# ASSETS.md — مستندات دارایی‌های بصری فال فرزانه

## خلاصه

فال فرزانه **هیچ تصویر بیرونی، هیچ اسکن از دستهٔ تجاری تاروت، و هیچ درخواست شبکه‌ای برای بارگذاری تصویر** ندارد.
تمام نمای کارت‌ها (پشت و رو) به‌صورت **اصلی و اختصاصی**، با CSS و نمادهای یونیکد عمومی، به‌صورت زنده در مرورگر ساخته می‌شوند.

این تصمیم به دو دلیل گرفته شده:

1. **کپی‌رایت:** دستهٔ کلاسیک رایدر–ویت–اسمیت (نسخهٔ اصلی چاپ‌شده در ۱۹۰۹) در برخی حوزه‌های قضایی وارد مالکیت عمومی شده،
   اما نسخه‌های رنگی، اسکن‌شده یا بازتولیدشدهٔ مدرن آن اغلب توسط ناشران معاصر دارای کپی‌رایت‌اند. توزیع مجدد این تصاویر
   بدون مجوز صریح، ریسک نقض حق نشر دارد.
2. **پایداری فنی:** طبق بخش ۱۴ و ۵۸ سند طراحی، برنامه باید بدون وابستگی به آدرس‌های تصویر بیرونی و به‌صورت کاملاً
   قابل‌اجرا روی GitHub Pages کار کند؛ یک طرح CSS جایگزین، این هدف را تضمین می‌کند.

## نحوهٔ ساخت نمای کارت‌ها

هر کارت (`js/ui.js` → `buildCardElement`) شامل:

* یک **پشت کارت اصلی** (`.card-back`): طرحی هندسی با الگوی خطوط طلایی تکرارشونده و نشان دایره‌ای مرکزی، کاملاً متفاوت
  از پشت هر دستهٔ تجاری شناخته‌شده.
* یک **روی کارت** (`.card-face`): کادر تزئینی طلایی، شمارهٔ کارت (برای آرکانای کبیر)، یک نماد یونیکد عمومی
  (مانند ✨ 🌙 ⚖️ 🗡️ 💧) متناظر با موضوع کارت، نام فارسی و انگلیسی کارت.

نمادهای یونیکد استفاده‌شده (ایموجی‌های استاندارد Unicode/Emoji) دارایی هیچ شخص یا شرکتی نیستند و بخشی از استاندارد
یونیکد به‌شمار می‌روند؛ رندر بصری آن‌ها توسط سیستم‌عامل/مرورگر کاربر انجام می‌شود و هیچ فایل تصویری در این پروژه
توزیع نمی‌شود.

## اگر می‌خواهید تصاویر واقعی/نقاشی‌شده جایگزین کنید

سیستم کاملاً **قابل جایگزینی** طراحی شده (بخش ۱۴ سند):

1. هر رکورد در `data/cards.js` دارای فیلد `image` است (مثلاً `assets/cards/major-00.svg`).
2. فایل تصویر واقعی خود را (SVG/PNG با مجوز مناسب یا هنر اورجینال) در پوشهٔ `assets/cards/` با همان نام قرار دهید.
3. در `js/ui.js`، تابع `buildCardElement` را می‌توان طوری تغییر داد که در صورت وجود فایل تصویر، به‌جای گلیف CSS از
   `<img src="{card.image}">` استفاده کند و در صورت خطای بارگذاری (`onerror`)، به طرح CSS فعلی بازگردد — دقیقاً طبق
   الزام «سقوط زیبا» در بخش ۵۸ سند.
4. **هرگز** تصاویر دارای کپی‌رایت را بدون مجوز کتبی در این پوشه قرار ندهید. گزینه‌های ایمن: هنر اورجینال شما،
   تصاویر با مجوز CC0 / Public Domain مشخص‌شده، یا تصاویر خریداری‌شده با مجوز توزیع دیجیتال.

## فونت

* **Vazirmatn** — فونت فارسی متن‌باز (مجوز SIL Open Font License 1.1)، از طریق CDN عمومی cdnjs بارگذاری می‌شود.
  منبع: https://github.com/rastikerdar/vazirmatn

## آیکون‌ها

آیکون‌های رابط کاربری (ناوبری پایین، دکمه‌ها) از نمادهای استاندارد یونیکد/ایموجی استفاده می‌کنند — بدون بستهٔ آیکون
بیرونی و بدون فایل گرافیکی اضافه.
